mongo:
  host: localhost
  port: 27017
  username: admin
  password: 3AY94J3UIO2TmRJ6tK
  database: liminal-field
  options:
    authSource: admin

minio:
  endpoint: localhost
  port: 9000
  useSSL: false
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: draft-assets

content:
  # 相对 server 进程 cwd（`pnpm start:dev` 在 server/ 下执行 → 位于 server/liminal-field-kb）
  repoRoot: ./liminal-field-kb

mineru:
  baseUrl: https://mineru.net
  token: ${MINERU_TOKEN}
  modelVersion: pipeline
  language: ch
  enableFormula: true
  enableTable: true
  pollIntervalMs: 3000
  pollTimeoutMs: 600000
