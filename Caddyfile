{$SITE_DOMAIN:localhost} {
    # Caddy 默认会去掉 Accept-Encoding 让上游不压缩(便于改 header),
    # 然后自己重新压。不显式 encode 就会裸传——2.4MB bundle 实测 8s
    # 首屏。开 zstd+gzip 后约 2.5s,3 倍提速。zstd 优先,客户端不支持
    # 自动回退 gzip。
    encode zstd gzip

    reverse_proxy client:80
}
